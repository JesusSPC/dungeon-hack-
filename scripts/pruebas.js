function check(x, y){

  let upY = toIndex(x, y - 1);
  let downY = toIndex(x, y + 1);
  let leftX = toIndex(x - 1, y);
  let rightX = toIndex(x + 1, y);

  switch (){
    case upY:
      checkFloor();
      checkDoor();
      checkKey();
  }
}
  function checkFloor(upY, downY, leftX, rightX){

  function checkDoor(v1, v2, v3, v4){

  }
  
  function checkKey(v1, v2, v3, v4){

  }
}

// check(player.tileX, player.tileY);
